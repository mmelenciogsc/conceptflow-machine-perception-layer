<?xml version="1.0" encoding="utf-8"?>
<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature android:name="android.hardware.wifi.direct" android:required="false" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@drawable/ic_app_icon"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/ic_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConceptFlow.Host"
        android:usesCleartextTraffic="false">
        <!-- Poco exposes this DSP RPC transport in /vendor/etc/public.libraries.txt. Optional
             keeps non-Qualcomm installs valid while granting Android 12+ linker-namespace access
             when the platform provides it. The library is never packaged by this application. -->
        <uses-native-library
            android:name="libcdsprpc.so"
            android:required="false" />
        <service
            android:name="org.conceptflow.mpl.host.AndroidNodeForegroundService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
        <!-- Keep GenieX's QNN/GGML libraries out of the real-time YOLO/depth process. This
             prevents two independently versioned Qualcomm runtime stacks sharing one linker
             namespace while retaining the same private application UID and storage boundary. -->
        <service
            android:name="org.conceptflow.mpl.host.vision.LocalVlmInferenceService"
            android:exported="false"
            android:process=":local_vlm" />
        <activity
            android:name="org.conceptflow.mpl.androidhost.LauncherActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
